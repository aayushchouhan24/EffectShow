export const effectNamesList = [
  "Rainbow Color Map",
  "Random Color Map",
  "Noise Color Map",
  "Palette Quantization",
  "Voronoi Mosaic",
  "Triangle Mosaic",
  "Heavy Film Grain",
  "Salt & Pepper",
  "Luma Noise",
  "Scanline Glitch",
  "Aperture Grille",
  "Glitch Blocks",
  "Bayer Dither",
  "Invert",
  "Black & White",
  "Sepia",
  "Posterize",
  "Solarize",
  "Noir",
  "Retro",
  "CRT (Intense)",
  "RGB Split",
  "CMYK Split",
  "Color Shift",
  "Hue Rotate",
  "Color Swap",
  "Palette Swap (Neon Pink/Green)",
  "Palette Swap (Cyber Yellow)",
  "Palette Swap (Toxic Matrix)",
  "Palette Swap (Blood Moon)",
  "Palette Swap (Synthwave)",
  "Palette Swap (Miami)",
  "Palette Swap (Deep Sea)",
  "Palette Swap (Magma High)",
  "Palette Swap (Void)",
  "Palette Swap (Gold Leaf)",
  "False Colors",
  "Color Reduction",
  "Contrast Fade",
  "Warm Filter",
  "Cool Filter",
  "Red Filter",
  "Green Filter",
  "Blue Filter",
  "Cyan Filter",
  "Magenta Filter",
  "Yellow Filter",
  "Orange Filter",
  "Purple Filter",
  "Neon",
  "Cyberpunk",
  "Vaporwave",
  "Psychedelic",
  "X-Ray",
  "Heatmap",
  "Blueprint",
  "Sketch",
  "Pencil",
  "Ink",
  "Cartoon",
  "Comic",
  "Mosaic",
  "Pixelate",
  "Pixel Art",
  "8-Bit",
  "Low Resolution",
  "Halftone (B&W)",
  "Halftone (Colored)",
  "Dot Screen (B&W)",
  "Dot Screen (Colored)",
  "Noise",
  "TV Noise",
  "Scanlines",
  "VHS Noise",
  "JPEG Artifacts",
  "Blur",
  "Gaussian Blur",
  "Motion Blur",
  "Radial Blur",
  "Frosted Glass",
  "Emboss",
  "Sharpen",
  "Glitch Mosaic",
  "Real Liquid Chrome",
  "Heat Vision",
  "Ink Wash",
  "Hyperspace",
  "Time Echo",
  "Datamosh",
  "Matrix Rain",
  "Night Vision",
  "Acid Wash",
  "Edge Style 1",
  "Edge Style 2",
  "Neon Pink Edge",
  "Neon Blue Edge",
  "Neon Green Edge",
  "Neon Yellow Edge",
  "Neon Purple Edge",
  "Neon Orange Edge",
  "Neon Cyan Edge",
  "Neon Red Edge",
  "Neon Magenta Edge",
  "Neon White Edge",
  "Noir Pixel",
  "Neon Pixel",
  "Acid Pixel",
  "Invert Pixel",
  "Sepia Pixel",
  "Toxic Pixel",
  "Fire Pixel",
  "Ice Pixel",
  "Gold Pixel",
  "Cyber Pixel",
  "Noir Scan",
  "Neon Scan",
  "Acid Scan",
  "Invert Scan",
  "Sepia Scan",
  "Toxic Scan",
  "Fire Scan",
  "Ice Scan",
  "Gold Scan",
  "Cyber Scan",
  "Noir Offset",
  "Neon Offset",
  "Acid Offset",
  "Invert Offset",
  "Sepia Offset",
  "Toxic Offset",
  "Fire Offset",
  "Ice Offset",
  "Gold Offset",
  "Cyber Offset",
  "Noir Dither",
  "Neon Dither",
  "Acid Dither",
  "Invert Dither",
  "Sepia Dither",
  "Toxic Dither",
  "Fire Dither",
  "Ice Dither",
  "Gold Dither",
  "Cyber Dither",
  "Noir Edge",
  "Neon Edge",
  "Acid Edge",
  "Invert Edge",
  "Sepia Edge",
  "Toxic Edge",
  "Fire Edge",
  "Ice Edge",
  "Gold Edge",
  "Cyber Edge",
  "Noir Chroma",
  "Neon Chroma",
  "Acid Chroma",
  "Invert Chroma",
  "Sepia Chroma",
  "Toxic Chroma",
  "Gold Chroma"
];

export const effectShadersList = [
  "float l = dot(baseColor.rgb, vec3(0.333)); outColor.rgb = 0.5 + 0.5 * cos(6.28318 * (l + vec3(0.0, 0.33, 0.67)));",
  "float l = dot(baseColor.rgb, vec3(0.333)); outColor.rgb = vec3(fract(sin(l * 100.0)*43758.5), fract(sin(l * 200.0)*43758.5), fract(sin(l * 300.0)*43758.5));",
  "float l = dot(baseColor.rgb, vec3(0.333)); outColor.rgb = baseColor.rgb + vec3((fract(sin(l * 4375.0 + uTime)*43758.5) - 0.5)*0.5);",
  "outColor.rgb = floor(baseColor.rgb * 3.0 + 0.5) / 3.0;",
  "\n    vec2 vUv = effectUv * 20.0; vec2 fl = floor(vUv); vec2 fr = fract(vUv);\n    float md = 2.0; vec2 mg;\n    for(int j=-1;j<=1;j++){\n        for(int i=-1;i<=1;i++){\n            vec2 g = vec2(float(i),float(j));\n            vec2 p = fract(sin(vec2(dot(fl+g,vec2(127.1,311.7)), dot(fl+g,vec2(269.5,183.3))))*43758.5);\n            float d = distance(g+p,fr);\n            if(d<md){md=d; mg=g;}\n        }\n    }\n    outColor.rgb = texture2D(uVideo, (fl + mg) / 20.0).rgb;\n",
  "\n    vec2 tUv = effectUv * 40.0; vec2 fl = floor(tUv); vec2 fr = fract(tUv);\n    float flip = mod(fl.x + fl.y, 2.0);\n    vec2 tr = fr; if(flip > 0.5) tr = vec2(1.0) - fr;\n    vec2 ofs = tr.x > tr.y ? vec2(0.66, 0.33) : vec2(0.33, 0.66);\n    if(flip > 0.5) ofs = vec2(1.0) - ofs;\n    outColor.rgb = texture2D(uVideo, (fl + ofs)/40.0).rgb;\n",
  "float fg = fract(sin(dot(effectUv * uTime, vec2(12.9898,78.233))) * 43758.5453); outColor.rgb = baseColor.rgb - fg * 0.4;",
  "float sp = fract(sin(dot(effectUv + uTime, vec2(12.9, 78.2))) * 43758.5); if(sp > 0.98) outColor.rgb = vec3(1.0); else if(sp < 0.02) outColor.rgb = vec3(0.0);",
  "float ln = dot(baseColor.rgb, vec3(0.333)); float lnn = fract(sin(dot(effectUv * uTime, vec2(12.9, 78.2))) * 43758.5) - 0.5; outColor.rgb = vec3(clamp(ln + lnn * 0.6, 0.0, 1.0));",
  "\n    vec2 sgUv = effectUv;\n    if(fract(sin(uTime * 10.0 + floor(sgUv.y * 30.0)) * 43758.5) > 0.95) sgUv.x += 0.05;\n    outColor.rgb = texture2D(uVideo, sgUv).rgb * (1.0 - sin(sgUv.y * uResolution.y * 1.5) * 0.3);\n",
  "\n    float agX = fract(effectUv.x * uResolution.x / 3.0);\n    vec3 ag = vec3(agX<0.33?1.0:0.0, agX>=0.33&&agX<0.66?1.0:0.0, agX>=0.66?1.0:0.0);\n    outColor.rgb = baseColor.rgb * ag * 2.0;\n",
  "\n    float tStep = floor(uTime * 15.0);\n    vec2 grid = floor(effectUv * 6.0);\n    float rVal = fract(sin(dot(grid, vec2(12.9898, 78.233)) + tStep * 123.456) * 43758.5453);\n    if (rVal > 0.5) {\n        int fx = int(mod(rVal * 100.0, 5.0));\n        if (fx == 0) outColor.rgb = 1.0 - baseColor.rgb;\n        else if (fx == 1) outColor.rgb = vec3(dot(baseColor.rgb, vec3(0.333)));\n        else if (fx == 2) outColor.rgb = baseColor.bgr;\n        else if (fx == 3) outColor.rgb = floor(baseColor.rgb * 3.0) / 3.0;\n        else outColor.rgb = vec3(baseColor.r, 0.0, 0.0);\n    }\n",
  "\n    vec2 fsUv = effectUv * uResolution.xy;\n    float mx = mod(fsUv.x, 4.0); float my = mod(fsUv.y, 4.0);\n    float dThresh = 0.0;\n    if(mx<1.0&&my<1.0) dThresh=0.0; else if(mx>=1.0&&mx<2.0&&my<1.0) dThresh=8.0; else if(mx>=2.0&&mx<3.0&&my<1.0) dThresh=2.0; else if(mx>=3.0&&my<1.0) dThresh=10.0;\n    else if(mx<1.0&&my>=1.0&&my<2.0) dThresh=12.0; else if(mx>=1.0&&mx<2.0&&my>=1.0&&my<2.0) dThresh=4.0; else if(mx>=2.0&&mx<3.0&&my>=1.0&&my<2.0) dThresh=14.0; else if(mx>=3.0&&my>=1.0&&my<2.0) dThresh=6.0;\n    else if(mx<1.0&&my>=2.0&&my<3.0) dThresh=3.0; else if(mx>=1.0&&mx<2.0&&my>=2.0&&my<3.0) dThresh=11.0; else if(mx>=2.0&&mx<3.0&&my>=2.0&&my<3.0) dThresh=1.0; else if(mx>=3.0&&my>=2.0&&my<3.0) dThresh=9.0;\n    else if(mx<1.0&&my>=3.0) dThresh=15.0; else if(mx>=1.0&&mx<2.0&&my>=3.0) dThresh=7.0; else if(mx>=2.0&&mx<3.0&&my>=3.0) dThresh=13.0; else dThresh=5.0;\n    dThresh /= 16.0;\n    outColor.rgb = vec3(step(dThresh, dot(baseColor.rgb, vec3(0.333))));\n",
  "outColor.rgb = 1.0 - baseColor.rgb;",
  "float bw = dot(baseColor.rgb, vec3(0.333)); outColor.rgb = vec3(bw);",
  "outColor.r = dot(baseColor.rgb, vec3(0.393, 0.769, 0.189)); outColor.g = dot(baseColor.rgb, vec3(0.349, 0.686, 0.168)); outColor.b = dot(baseColor.rgb, vec3(0.272, 0.534, 0.131));",
  "outColor.rgb = floor(baseColor.rgb * 4.0) / 4.0;",
  "outColor.rgb = mix(baseColor.rgb, 1.0 - baseColor.rgb, step(0.5, dot(baseColor.rgb, vec3(0.333))));",
  "float n = dot(baseColor.rgb, vec3(0.299, 0.587, 0.114)); outColor.rgb = vec3(pow(n, 1.5)) * vec3(0.9, 0.9, 1.0);",
  "outColor.rgb = baseColor.rgb * vec3(0.9, 0.8, 0.6) + vec3(0.1, 0.05, 0.0);",
  "\n    vec2 crtUv = effectUv - 0.5;\n    crtUv *= 1.1 + dot(crtUv, crtUv) * 0.2;\n    crtUv += 0.5;\n    if(crtUv.x < 0.0 || crtUv.x > 1.0 || crtUv.y < 0.0 || crtUv.y > 1.0) {\n        outColor.rgb = vec3(0.0);\n    } else {\n        outColor.r = texture2D(uVideo, crtUv + vec2(0.005, 0.0)).r;\n        outColor.g = texture2D(uVideo, crtUv).g;\n        outColor.b = texture2D(uVideo, crtUv - vec2(0.005, 0.0)).b;\n        float scanline = sin(crtUv.y * uResolution.y * 3.0) * 0.2;\n        outColor.rgb -= scanline;\n        outColor.rgb *= 1.0 - pow(distance(crtUv, vec2(0.5)), 2.0) * 0.5;\n    }\n",
  "outColor.r = texture2D(uVideo, effectUv + vec2(0.02, 0.0)).r; outColor.g = baseColor.g; outColor.b = texture2D(uVideo, effectUv - vec2(0.02, 0.0)).b;",
  "\n    vec4 c1 = texture2D(uVideo, effectUv + vec2(0.015, 0.015));\n    vec4 c2 = texture2D(uVideo, effectUv - vec2(0.015, 0.015));\n    vec4 c3 = texture2D(uVideo, effectUv + vec2(-0.015, 0.015));\n    outColor.r = c1.r * 0.5 + baseColor.r * 0.5;\n    outColor.g = c2.g * 0.5 + baseColor.g * 0.5;\n    outColor.b = c3.b * 0.5 + baseColor.b * 0.5;\n",
  "outColor.rgb = vec3(baseColor.g, baseColor.b, baseColor.r);",
  "\n    float angle = uTime * 2.0;\n    float s = sin(angle), c = cos(angle);\n    vec3 weights = (vec3(2.0 * c, -sqrt(3.0) * s - c, sqrt(3.0) * s - c) + 1.0) / 3.0;\n    outColor.r = dot(baseColor.rgb, weights.xyz);\n    outColor.g = dot(baseColor.rgb, weights.zxy);\n    outColor.b = dot(baseColor.rgb, weights.yzx);\n",
  "outColor.rgb = vec3(baseColor.b, baseColor.r, baseColor.g);",
  "float l = dot(baseColor.rgb, vec3(0.333)); outColor.rgb = mix(vec3(0.0, 1.0, 0.4), vec3(1.0, 0.0, 0.6), step(0.5, l));",
  "float l = dot(baseColor.rgb, vec3(0.333)); outColor.rgb = mix(vec3(0.0, 0.2, 1.0), vec3(1.0, 1.0, 0.0), step(0.5, l));",
  "float l = dot(baseColor.rgb, vec3(0.333)); outColor.rgb = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 1.0, 0.2), step(0.5, l));",
  "float l = dot(baseColor.rgb, vec3(0.333)); outColor.rgb = mix(vec3(0.0, 0.0, 0.0), vec3(1.0, 0.0, 0.0), step(0.5, l));",
  "float l = dot(baseColor.rgb, vec3(0.333)); outColor.rgb = mix(vec3(0.0, 1.0, 1.0), vec3(1.0, 0.0, 1.0), step(0.5, l));",
  "float l = dot(baseColor.rgb, vec3(0.333)); outColor.rgb = mix(vec3(1.0, 0.5, 0.0), vec3(0.0, 1.0, 0.8), step(0.5, l));",
  "float l = dot(baseColor.rgb, vec3(0.333)); outColor.rgb = mix(vec3(0.0, 0.0, 0.2), vec3(0.0, 1.0, 1.0), step(0.5, l));",
  "float l = dot(baseColor.rgb, vec3(0.333)); outColor.rgb = mix(vec3(0.2, 0.0, 0.0), vec3(1.0, 0.8, 0.0), step(0.5, l));",
  "float l = dot(baseColor.rgb, vec3(0.333)); outColor.rgb = mix(vec3(0.0, 0.0, 0.0), vec3(1.0, 1.0, 1.0), step(0.5, l));",
  "float l = dot(baseColor.rgb, vec3(0.333)); outColor.rgb = mix(vec3(0.1, 0.0, 0.0), vec3(1.0, 0.8, 0.2), step(0.5, l));",
  "float fc = dot(baseColor.rgb, vec3(0.333)); outColor.rgb = vec3(fc * 2.0, fc, 1.0 - fc);",
  "outColor.rgb = floor(baseColor.rgb * 2.0) / 2.0;",
  "outColor.rgb = (baseColor.rgb - 0.5) * 0.5 + 0.5;",
  "outColor.rgb = baseColor.rgb * vec3(1.1, 1.0, 0.8);",
  "outColor.rgb = baseColor.rgb * vec3(0.8, 1.0, 1.2);",
  "outColor.rgb = baseColor.rgb * vec3(1.0, 0.0, 0.0);",
  "outColor.rgb = baseColor.rgb * vec3(0.0, 1.0, 0.0);",
  "outColor.rgb = baseColor.rgb * vec3(0.0, 0.0, 1.0);",
  "outColor.rgb = baseColor.rgb * vec3(0.0, 1.0, 1.0);",
  "outColor.rgb = baseColor.rgb * vec3(1.0, 0.0, 1.0);",
  "outColor.rgb = baseColor.rgb * vec3(1.0, 1.0, 0.0);",
  "outColor.rgb = baseColor.rgb * vec3(1.0, 0.5, 0.0);",
  "outColor.rgb = baseColor.rgb * vec3(0.5, 0.0, 1.0);",
  "float edge = distance(baseColor.rgb, texture2D(uVideo, effectUv + vec2(0.005)).rgb); outColor.rgb = mix(baseColor.rgb, vec3(0.0, 1.0, 1.0), edge * 10.0);",
  "outColor.rgb = mix(vec3(1.0, 0.0, 0.5), vec3(0.0, 1.0, 1.0), dot(baseColor.rgb, vec3(0.333)));",
  "outColor.rgb = mix(vec3(0.0, 1.0, 1.0), vec3(1.0, 0.5, 0.8), baseColor.b);",
  "outColor.rgb = sin(baseColor.rgb * 20.0 + uTime * 5.0) * 0.5 + 0.5;",
  "float xr = dot(baseColor.rgb, vec3(0.333)); outColor.rgb = vec3(1.0 - xr) * vec3(0.5, 0.9, 1.0);",
  "float hm = dot(baseColor.rgb, vec3(0.333)); outColor.rgb = vec3(smoothstep(0.5, 1.0, hm), smoothstep(0.0, 0.5, hm), smoothstep(0.0, 0.25, 1.0 - hm));",
  "float eBP = distance(baseColor.rgb, texture2D(uVideo, effectUv + vec2(0.005)).rgb); outColor.rgb = mix(vec3(0.0, 0.2, 0.5), vec3(1.0), eBP * 15.0);",
  "float eSK = distance(baseColor.rgb, texture2D(uVideo, effectUv + vec2(0.003)).rgb); outColor.rgb = vec3(1.0 - eSK * 10.0);",
  "float ePC = distance(baseColor.rgb, texture2D(uVideo, effectUv + vec2(0.002)).rgb); outColor.rgb = vec3(1.0) - vec3(ePC * 15.0);",
  "float eIN = distance(baseColor.rgb, texture2D(uVideo, effectUv + vec2(0.005)).rgb); outColor.rgb = eIN > 0.1 ? vec3(0.0) : vec3(1.0);",
  "float eCT = distance(baseColor.rgb, texture2D(uVideo, effectUv + vec2(0.004)).rgb); outColor.rgb = eCT > 0.1 ? vec3(0.0) : floor(baseColor.rgb * 5.0) / 5.0;",
  "float eCM = distance(baseColor.rgb, texture2D(uVideo, effectUv + vec2(0.003)).rgb); vec3 pCM = floor(baseColor.rgb * 4.0) / 4.0; outColor.rgb = eCM > 0.08 ? vec3(0.0) : pCM;",
  "vec2 mUv = floor(effectUv * 50.0) / 50.0; outColor.rgb = texture2D(uVideo, mUv).rgb;",
  "vec2 pUv = floor(effectUv * 80.0) / 80.0; outColor.rgb = texture2D(uVideo, pUv).rgb;",
  "vec2 paUv = floor(effectUv * 64.0) / 64.0; outColor.rgb = floor(texture2D(uVideo, paUv).rgb * 4.0) / 4.0;",
  "vec2 e8Uv = floor(effectUv * 128.0) / 128.0; outColor.rgb = floor(texture2D(uVideo, e8Uv).rgb * 8.0) / 8.0;",
  "vec2 lUv = floor(effectUv * 32.0) / 32.0; outColor.rgb = texture2D(uVideo, lUv).rgb;",
  "\n    float htLuma = dot(baseColor.rgb, vec3(0.333));\n    float htRadius = sin(effectUv.x * 500.0) * cos(effectUv.y * 500.0);\n    outColor.rgb = vec3(htLuma > htRadius ? 1.0 : 0.0);\n",
  "\n    float hcR = sin(effectUv.x * 400.0) * cos(effectUv.y * 400.0);\n    float hcG = sin(effectUv.x * 400.0 + 1.0) * cos(effectUv.y * 400.0 + 1.0);\n    float hcB = sin(effectUv.x * 400.0 + 2.0) * cos(effectUv.y * 400.0 + 2.0);\n    outColor.r = baseColor.r > hcR ? baseColor.r : 0.0;\n    outColor.g = baseColor.g > hcG ? baseColor.g : 0.0;\n    outColor.b = baseColor.b > hcB ? baseColor.b : 0.0;\n",
  "\n    float dsDist = distance(fract(effectUv * 100.0), vec2(0.5));\n    outColor.rgb = vec3(dsDist < dot(baseColor.rgb, vec3(0.333)) ? 1.0 : 0.0);\n",
  "\n    float dsDist2 = distance(fract(effectUv * 100.0), vec2(0.5));\n    outColor.r = dsDist2 < baseColor.r ? baseColor.r : 0.0;\n    outColor.g = dsDist2 < baseColor.g ? baseColor.g : 0.0;\n    outColor.b = dsDist2 < baseColor.b ? baseColor.b : 0.0;\n",
  "float nF = fract(sin(dot(effectUv, vec2(12.9898, 78.233))) * 43758.5453); outColor.rgb = baseColor.rgb + vec3(nF * 0.4 - 0.2);",
  "float tvN = fract(sin(dot(effectUv * uTime, vec2(12.9, 78.2))) * 43758.5); outColor.rgb = mix(baseColor.rgb, vec3(tvN), 0.5);",
  "float sl = sin(effectUv.y * uResolution.y * 1.5); outColor.rgb = baseColor.rgb * (1.0 - sl * 0.3);",
  "\n    float vhsN = fract(sin(dot(vec2(effectUv.x, uTime), vec2(12.9, 78.2))) * 43758.5);\n    vec2 vhsUv = effectUv;\n    vhsUv.x += vhsN * 0.01;\n    outColor.rgb = texture2D(uVideo, vhsUv).rgb;\n",
  "vec2 jpgUv = floor(effectUv * uResolution.xy / 8.0) * 8.0 / uResolution.xy; outColor.rgb = texture2D(uVideo, jpgUv).rgb;",
  "outColor.rgb = (texture2D(uVideo, effectUv + vec2(0.005, 0.0)).rgb + texture2D(uVideo, effectUv - vec2(0.005, 0.0)).rgb + texture2D(uVideo, effectUv + vec2(0.0, 0.005)).rgb + texture2D(uVideo, effectUv - vec2(0.0, 0.005)).rgb) * 0.25;",
  "outColor.rgb = baseColor.rgb * 0.4 + (texture2D(uVideo, effectUv + vec2(0.01, 0.0)).rgb + texture2D(uVideo, effectUv - vec2(0.01, 0.0)).rgb + texture2D(uVideo, effectUv + vec2(0.0, 0.01)).rgb + texture2D(uVideo, effectUv - vec2(0.0, 0.01)).rgb) * 0.15;",
  "outColor.rgb = (texture2D(uVideo, effectUv).rgb + texture2D(uVideo, effectUv - vec2(0.01, 0.0)).rgb + texture2D(uVideo, effectUv - vec2(0.02, 0.0)).rgb) * 0.333;",
  "vec2 dirRB = effectUv - 0.5; outColor.rgb = (texture2D(uVideo, effectUv).rgb + texture2D(uVideo, effectUv - dirRB * 0.02).rgb + texture2D(uVideo, effectUv - dirRB * 0.04).rgb) * 0.333;",
  "vec2 fgUv = effectUv + vec2(fract(sin(dot(effectUv, vec2(12.9, 78.2))) * 43758.5) - 0.5) * 0.02; outColor.rgb = texture2D(uVideo, fgUv).rgb;",
  "vec3 emb = texture2D(uVideo, effectUv + vec2(0.002)).rgb - texture2D(uVideo, effectUv - vec2(0.002)).rgb; outColor.rgb = vec3(0.5) + emb;",
  "vec3 shp = texture2D(uVideo, effectUv).rgb * 5.0 - (texture2D(uVideo, effectUv + vec2(0.002, 0.0)).rgb + texture2D(uVideo, effectUv - vec2(0.002, 0.0)).rgb + texture2D(uVideo, effectUv + vec2(0.0, 0.002)).rgb + texture2D(uVideo, effectUv - vec2(0.0, 0.002)).rgb); outColor.rgb = shp;",
  "vec2 gmUv = effectUv; gmUv.x += step(0.9, fract(sin(uTime * 10.0 + effectUv.y * 5.0) * 43758.5)) * 0.05; gmUv = floor(gmUv * 40.0) / 40.0; outColor.rgb = texture2D(uVideo, gmUv).rgb;",
  "\n    vec2 lcUv = effectUv;\n    float nX = fract(sin(dot(effectUv + uTime, vec2(12.9, 78.2))) * 43758.5);\n    float nY = fract(cos(dot(effectUv - uTime, vec2(12.9, 78.2))) * 43758.5);\n    lcUv += vec2(nX - 0.5, nY - 0.5) * 0.05;\n    vec3 lcCol = texture2D(uVideo, lcUv).rgb;\n    float lcl = dot(lcCol, vec3(0.333));\n    outColor.rgb = vec3(smoothstep(0.4, 0.6, lcl)) * vec3(0.9, 0.95, 1.0);\n",
  "float hvL = dot(baseColor.rgb, vec3(0.333)); outColor.rgb = mix(vec3(0.0, 0.0, 1.0), mix(vec3(1.0, 0.0, 0.0), vec3(1.0, 1.0, 0.0), hvL), hvL);",
  "float iwL = dot(baseColor.rgb, vec3(0.333)); outColor.rgb = vec3(floor(iwL * 3.0) / 3.0) * vec3(0.9, 0.95, 1.0);",
  "vec2 hsUv = effectUv - 0.5; hsUv *= 1.0 - uAudioHigh * 0.5; hsUv += 0.5; outColor.rgb = texture2D(uVideo, hsUv).rgb * vec3(1.2, 1.0, 1.5);",
  "outColor.rgb = mix(baseColor.rgb, texture2D(uVideo, effectUv - vec2(0.02, 0.0)).rgb, 0.5);",
  "\n    vec2 dmuv = effectUv;\n    float dm = fract(sin(floor(uTime * 10.0) + effectUv.x * 50.0) * 43758.5);\n    dmuv.y += dm * 0.05;\n    outColor.rgb = texture2D(uVideo, dmuv).rgb;\n",
  "\n    float mxRain = fract(sin(floor(effectUv.x * 50.0) * 10.0 + uTime * 2.0) * 43758.5);\n    float mxLuma = dot(baseColor.rgb, vec3(0.333));\n    outColor.rgb = vec3(0.0, mxLuma + mxRain * 0.5, 0.0);\n",
  "\n    float nvL = dot(baseColor.rgb, vec3(0.333));\n    float nvScan = sin(effectUv.y * 100.0 + uTime * 10.0) * 0.1;\n    float nvNoise = fract(sin(dot(effectUv * uTime, vec2(12.9, 78.2))) * 43758.5) * 0.2;\n    outColor.rgb = vec3(0.0, nvL + nvScan + nvNoise, 0.0);\n",
  "\n    float awL = dot(baseColor.rgb, vec3(0.333));\n    outColor.r = sin(awL * 10.0 + uTime) * 0.5 + 0.5;\n    outColor.g = sin(awL * 15.0 + uTime) * 0.5 + 0.5;\n    outColor.b = sin(awL * 20.0 + uTime) * 0.5 + 0.5;\n",
  "\n    float esL1 = distance(baseColor.rgb, texture2D(uVideo, effectUv + vec2(0.001)).rgb);\n    outColor.rgb = mix(vec3(0.0), vec3(1.0, 1.0, 1.0), esL1 * 10.0);\n",
  "\n    float esL2 = distance(baseColor.rgb, texture2D(uVideo, effectUv + vec2(0.002)).rgb);\n    outColor.rgb = mix(vec3(0.0), vec3(0.0, 1.0, 0.5), esL2 * 12.0);\n",
  "\n        float neE = distance(baseColor.rgb, texture2D(uVideo, effectUv + vec2(0.005)).rgb);\n        outColor.rgb = mix(vec3(0.0), vec3(1.0, 0.2, 0.8), neE * 15.0);\n    ",
  "\n        float neE = distance(baseColor.rgb, texture2D(uVideo, effectUv + vec2(0.005)).rgb);\n        outColor.rgb = mix(vec3(0.0), vec3(0.2, 0.5, 1.0), neE * 15.0);\n    ",
  "\n        float neE = distance(baseColor.rgb, texture2D(uVideo, effectUv + vec2(0.005)).rgb);\n        outColor.rgb = mix(vec3(0.0), vec3(0.2, 1.0, 0.3), neE * 15.0);\n    ",
  "\n        float neE = distance(baseColor.rgb, texture2D(uVideo, effectUv + vec2(0.005)).rgb);\n        outColor.rgb = mix(vec3(0.0), vec3(1.0, 1.0, 0.1), neE * 15.0);\n    ",
  "\n        float neE = distance(baseColor.rgb, texture2D(uVideo, effectUv + vec2(0.005)).rgb);\n        outColor.rgb = mix(vec3(0.0), vec3(0.7, 0.2, 1.0), neE * 15.0);\n    ",
  "\n        float neE = distance(baseColor.rgb, texture2D(uVideo, effectUv + vec2(0.005)).rgb);\n        outColor.rgb = mix(vec3(0.0), vec3(1.0, 0.5, 0.1), neE * 15.0);\n    ",
  "\n        float neE = distance(baseColor.rgb, texture2D(uVideo, effectUv + vec2(0.005)).rgb);\n        outColor.rgb = mix(vec3(0.0), vec3(0.1, 1.0, 1.0), neE * 15.0);\n    ",
  "\n        float neE = distance(baseColor.rgb, texture2D(uVideo, effectUv + vec2(0.005)).rgb);\n        outColor.rgb = mix(vec3(0.0), vec3(1.0, 0.1, 0.1), neE * 15.0);\n    ",
  "\n        float neE = distance(baseColor.rgb, texture2D(uVideo, effectUv + vec2(0.005)).rgb);\n        outColor.rgb = mix(vec3(0.0), vec3(1.0, 0.0, 1.0), neE * 15.0);\n    ",
  "\n        float neE = distance(baseColor.rgb, texture2D(uVideo, effectUv + vec2(0.005)).rgb);\n        outColor.rgb = mix(vec3(0.0), vec3(1.0, 1.0, 1.0), neE * 15.0);\n    ",
  "vec2 uvM = floor(effectUv * 50.0) / 50.0; vec3 cM = texture2D(uVideo, uvM).rgb; float lM = dot(cM, vec3(0.333)); outColor.rgb = vec3(pow(lM, 1.5));",
  "vec2 uvM = floor(effectUv * 50.0) / 50.0; vec3 cM = texture2D(uVideo, uvM).rgb; outColor.rgb = mix(vec3(0.0, 1.0, 1.0), vec3(1.0, 0.0, 1.0), cM.r);",
  "vec2 uvM = floor(effectUv * 50.0) / 50.0; vec3 cM = texture2D(uVideo, uvM).rgb; outColor.rgb = mod(cM * 5.0 + uTime, 1.0);",
  "vec2 uvM = floor(effectUv * 50.0) / 50.0; vec3 cM = texture2D(uVideo, uvM).rgb; outColor.rgb = 1.0 - cM;",
  "vec2 uvM = floor(effectUv * 50.0) / 50.0; vec3 cM = texture2D(uVideo, uvM).rgb; outColor.r = dot(cM, vec3(0.393, 0.769, 0.189)); outColor.g = dot(cM, vec3(0.349, 0.686, 0.168)); outColor.b = dot(cM, vec3(0.272, 0.534, 0.131));",
  "vec2 uvM = floor(effectUv * 50.0) / 50.0; vec3 cM = texture2D(uVideo, uvM).rgb; outColor.rgb = mix(vec3(0.0, 0.2, 0.0), vec3(0.2, 1.0, 0.2), dot(cM, vec3(0.333)));",
  "vec2 uvM = floor(effectUv * 50.0) / 50.0; vec3 cM = texture2D(uVideo, uvM).rgb; outColor.rgb = mix(vec3(0.2, 0.0, 0.0), vec3(1.0, 0.8, 0.0), cM.r);",
  "vec2 uvM = floor(effectUv * 50.0) / 50.0; vec3 cM = texture2D(uVideo, uvM).rgb; outColor.rgb = mix(vec3(0.0, 0.1, 0.3), vec3(0.8, 1.0, 1.0), cM.b);",
  "vec2 uvM = floor(effectUv * 50.0) / 50.0; vec3 cM = texture2D(uVideo, uvM).rgb; outColor.rgb = mix(vec3(0.2, 0.1, 0.0), vec3(1.0, 0.9, 0.4), dot(cM, vec3(0.333)));",
  "vec2 uvM = floor(effectUv * 50.0) / 50.0; vec3 cM = texture2D(uVideo, uvM).rgb; outColor.rgb = mix(vec3(0.0, 0.0, 0.3), vec3(0.0, 1.0, 1.0), cM.g);",
  "vec2 uvM = effectUv; vec3 cM = texture2D(uVideo, uvM).rgb; cM -= sin(uvM.y * 800.0) * 0.1; float lM = dot(cM, vec3(0.333)); outColor.rgb = vec3(pow(lM, 1.5));",
  "vec2 uvM = effectUv; vec3 cM = texture2D(uVideo, uvM).rgb; cM -= sin(uvM.y * 800.0) * 0.1; outColor.rgb = mix(vec3(0.0, 1.0, 1.0), vec3(1.0, 0.0, 1.0), cM.r);",
  "vec2 uvM = effectUv; vec3 cM = texture2D(uVideo, uvM).rgb; cM -= sin(uvM.y * 800.0) * 0.1; outColor.rgb = mod(cM * 5.0 + uTime, 1.0);",
  "vec2 uvM = effectUv; vec3 cM = texture2D(uVideo, uvM).rgb; cM -= sin(uvM.y * 800.0) * 0.1; outColor.rgb = 1.0 - cM;",
  "vec2 uvM = effectUv; vec3 cM = texture2D(uVideo, uvM).rgb; cM -= sin(uvM.y * 800.0) * 0.1; outColor.r = dot(cM, vec3(0.393, 0.769, 0.189)); outColor.g = dot(cM, vec3(0.349, 0.686, 0.168)); outColor.b = dot(cM, vec3(0.272, 0.534, 0.131));",
  "vec2 uvM = effectUv; vec3 cM = texture2D(uVideo, uvM).rgb; cM -= sin(uvM.y * 800.0) * 0.1; outColor.rgb = mix(vec3(0.0, 0.2, 0.0), vec3(0.2, 1.0, 0.2), dot(cM, vec3(0.333)));",
  "vec2 uvM = effectUv; vec3 cM = texture2D(uVideo, uvM).rgb; cM -= sin(uvM.y * 800.0) * 0.1; outColor.rgb = mix(vec3(0.2, 0.0, 0.0), vec3(1.0, 0.8, 0.0), cM.r);",
  "vec2 uvM = effectUv; vec3 cM = texture2D(uVideo, uvM).rgb; cM -= sin(uvM.y * 800.0) * 0.1; outColor.rgb = mix(vec3(0.0, 0.1, 0.3), vec3(0.8, 1.0, 1.0), cM.b);",
  "vec2 uvM = effectUv; vec3 cM = texture2D(uVideo, uvM).rgb; cM -= sin(uvM.y * 800.0) * 0.1; outColor.rgb = mix(vec3(0.2, 0.1, 0.0), vec3(1.0, 0.9, 0.4), dot(cM, vec3(0.333)));",
  "vec2 uvM = effectUv; vec3 cM = texture2D(uVideo, uvM).rgb; cM -= sin(uvM.y * 800.0) * 0.1; outColor.rgb = mix(vec3(0.0, 0.0, 0.3), vec3(0.0, 1.0, 1.0), cM.g);",
  "vec2 uvM = effectUv; uvM.x += sin(uvM.y * 20.0) * 0.02; vec3 cM = texture2D(uVideo, uvM).rgb; float lM = dot(cM, vec3(0.333)); outColor.rgb = vec3(pow(lM, 1.5));",
  "vec2 uvM = effectUv; uvM.x += sin(uvM.y * 20.0) * 0.02; vec3 cM = texture2D(uVideo, uvM).rgb; outColor.rgb = mix(vec3(0.0, 1.0, 1.0), vec3(1.0, 0.0, 1.0), cM.r);",
  "vec2 uvM = effectUv; uvM.x += sin(uvM.y * 20.0) * 0.02; vec3 cM = texture2D(uVideo, uvM).rgb; outColor.rgb = mod(cM * 5.0 + uTime, 1.0);",
  "vec2 uvM = effectUv; uvM.x += sin(uvM.y * 20.0) * 0.02; vec3 cM = texture2D(uVideo, uvM).rgb; outColor.rgb = 1.0 - cM;",
  "vec2 uvM = effectUv; uvM.x += sin(uvM.y * 20.0) * 0.02; vec3 cM = texture2D(uVideo, uvM).rgb; outColor.r = dot(cM, vec3(0.393, 0.769, 0.189)); outColor.g = dot(cM, vec3(0.349, 0.686, 0.168)); outColor.b = dot(cM, vec3(0.272, 0.534, 0.131));",
  "vec2 uvM = effectUv; uvM.x += sin(uvM.y * 20.0) * 0.02; vec3 cM = texture2D(uVideo, uvM).rgb; outColor.rgb = mix(vec3(0.0, 0.2, 0.0), vec3(0.2, 1.0, 0.2), dot(cM, vec3(0.333)));",
  "vec2 uvM = effectUv; uvM.x += sin(uvM.y * 20.0) * 0.02; vec3 cM = texture2D(uVideo, uvM).rgb; outColor.rgb = mix(vec3(0.2, 0.0, 0.0), vec3(1.0, 0.8, 0.0), cM.r);",
  "vec2 uvM = effectUv; uvM.x += sin(uvM.y * 20.0) * 0.02; vec3 cM = texture2D(uVideo, uvM).rgb; outColor.rgb = mix(vec3(0.0, 0.1, 0.3), vec3(0.8, 1.0, 1.0), cM.b);",
  "vec2 uvM = effectUv; uvM.x += sin(uvM.y * 20.0) * 0.02; vec3 cM = texture2D(uVideo, uvM).rgb; outColor.rgb = mix(vec3(0.2, 0.1, 0.0), vec3(1.0, 0.9, 0.4), dot(cM, vec3(0.333)));",
  "vec2 uvM = effectUv; uvM.x += sin(uvM.y * 20.0) * 0.02; vec3 cM = texture2D(uVideo, uvM).rgb; outColor.rgb = mix(vec3(0.0, 0.0, 0.3), vec3(0.0, 1.0, 1.0), cM.g);",
  "vec2 uvM = effectUv; vec3 cM = texture2D(uVideo, uvM).rgb; cM += (fract(sin(dot(uvM, vec2(12.9, 78.2))) * 43758.5) - 0.5) * 0.2; float lM = dot(cM, vec3(0.333)); outColor.rgb = vec3(pow(lM, 1.5));",
  "vec2 uvM = effectUv; vec3 cM = texture2D(uVideo, uvM).rgb; cM += (fract(sin(dot(uvM, vec2(12.9, 78.2))) * 43758.5) - 0.5) * 0.2; outColor.rgb = mix(vec3(0.0, 1.0, 1.0), vec3(1.0, 0.0, 1.0), cM.r);",
  "vec2 uvM = effectUv; vec3 cM = texture2D(uVideo, uvM).rgb; cM += (fract(sin(dot(uvM, vec2(12.9, 78.2))) * 43758.5) - 0.5) * 0.2; outColor.rgb = mod(cM * 5.0 + uTime, 1.0);",
  "vec2 uvM = effectUv; vec3 cM = texture2D(uVideo, uvM).rgb; cM += (fract(sin(dot(uvM, vec2(12.9, 78.2))) * 43758.5) - 0.5) * 0.2; outColor.rgb = 1.0 - cM;",
  "vec2 uvM = effectUv; vec3 cM = texture2D(uVideo, uvM).rgb; cM += (fract(sin(dot(uvM, vec2(12.9, 78.2))) * 43758.5) - 0.5) * 0.2; outColor.r = dot(cM, vec3(0.393, 0.769, 0.189)); outColor.g = dot(cM, vec3(0.349, 0.686, 0.168)); outColor.b = dot(cM, vec3(0.272, 0.534, 0.131));",
  "vec2 uvM = effectUv; vec3 cM = texture2D(uVideo, uvM).rgb; cM += (fract(sin(dot(uvM, vec2(12.9, 78.2))) * 43758.5) - 0.5) * 0.2; outColor.rgb = mix(vec3(0.0, 0.2, 0.0), vec3(0.2, 1.0, 0.2), dot(cM, vec3(0.333)));",
  "vec2 uvM = effectUv; vec3 cM = texture2D(uVideo, uvM).rgb; cM += (fract(sin(dot(uvM, vec2(12.9, 78.2))) * 43758.5) - 0.5) * 0.2; outColor.rgb = mix(vec3(0.2, 0.0, 0.0), vec3(1.0, 0.8, 0.0), cM.r);",
  "vec2 uvM = effectUv; vec3 cM = texture2D(uVideo, uvM).rgb; cM += (fract(sin(dot(uvM, vec2(12.9, 78.2))) * 43758.5) - 0.5) * 0.2; outColor.rgb = mix(vec3(0.0, 0.1, 0.3), vec3(0.8, 1.0, 1.0), cM.b);",
  "vec2 uvM = effectUv; vec3 cM = texture2D(uVideo, uvM).rgb; cM += (fract(sin(dot(uvM, vec2(12.9, 78.2))) * 43758.5) - 0.5) * 0.2; outColor.rgb = mix(vec3(0.2, 0.1, 0.0), vec3(1.0, 0.9, 0.4), dot(cM, vec3(0.333)));",
  "vec2 uvM = effectUv; vec3 cM = texture2D(uVideo, uvM).rgb; cM += (fract(sin(dot(uvM, vec2(12.9, 78.2))) * 43758.5) - 0.5) * 0.2; outColor.rgb = mix(vec3(0.0, 0.0, 0.3), vec3(0.0, 1.0, 1.0), cM.g);",
  "vec3 cM = vec3(distance(baseColor.rgb, texture2D(uVideo, effectUv + vec2(0.005)).rgb) * 5.0); float lM = dot(cM, vec3(0.333)); outColor.rgb = vec3(pow(lM, 1.5));",
  "vec3 cM = vec3(distance(baseColor.rgb, texture2D(uVideo, effectUv + vec2(0.005)).rgb) * 5.0); outColor.rgb = mix(vec3(0.0, 1.0, 1.0), vec3(1.0, 0.0, 1.0), cM.r);",
  "vec3 cM = vec3(distance(baseColor.rgb, texture2D(uVideo, effectUv + vec2(0.005)).rgb) * 5.0); outColor.rgb = mod(cM * 5.0 + uTime, 1.0);",
  "vec3 cM = vec3(distance(baseColor.rgb, texture2D(uVideo, effectUv + vec2(0.005)).rgb) * 5.0); outColor.rgb = 1.0 - cM;",
  "vec3 cM = vec3(distance(baseColor.rgb, texture2D(uVideo, effectUv + vec2(0.005)).rgb) * 5.0); outColor.r = dot(cM, vec3(0.393, 0.769, 0.189)); outColor.g = dot(cM, vec3(0.349, 0.686, 0.168)); outColor.b = dot(cM, vec3(0.272, 0.534, 0.131));",
  "vec3 cM = vec3(distance(baseColor.rgb, texture2D(uVideo, effectUv + vec2(0.005)).rgb) * 5.0); outColor.rgb = mix(vec3(0.0, 0.2, 0.0), vec3(0.2, 1.0, 0.2), dot(cM, vec3(0.333)));",
  "vec3 cM = vec3(distance(baseColor.rgb, texture2D(uVideo, effectUv + vec2(0.005)).rgb) * 5.0); outColor.rgb = mix(vec3(0.2, 0.0, 0.0), vec3(1.0, 0.8, 0.0), cM.r);",
  "vec3 cM = vec3(distance(baseColor.rgb, texture2D(uVideo, effectUv + vec2(0.005)).rgb) * 5.0); outColor.rgb = mix(vec3(0.0, 0.1, 0.3), vec3(0.8, 1.0, 1.0), cM.b);",
  "vec3 cM = vec3(distance(baseColor.rgb, texture2D(uVideo, effectUv + vec2(0.005)).rgb) * 5.0); outColor.rgb = mix(vec3(0.2, 0.1, 0.0), vec3(1.0, 0.9, 0.4), dot(cM, vec3(0.333)));",
  "vec3 cM = vec3(distance(baseColor.rgb, texture2D(uVideo, effectUv + vec2(0.005)).rgb) * 5.0); outColor.rgb = mix(vec3(0.0, 0.0, 0.3), vec3(0.0, 1.0, 1.0), cM.g);",
  "vec3 cM = vec3(texture2D(uVideo, effectUv + vec2(0.01, 0.0)).r, baseColor.g, texture2D(uVideo, effectUv - vec2(0.01, 0.0)).b); float lM = dot(cM, vec3(0.333)); outColor.rgb = vec3(pow(lM, 1.5));",
  "vec3 cM = vec3(texture2D(uVideo, effectUv + vec2(0.01, 0.0)).r, baseColor.g, texture2D(uVideo, effectUv - vec2(0.01, 0.0)).b); outColor.rgb = mix(vec3(0.0, 1.0, 1.0), vec3(1.0, 0.0, 1.0), cM.r);",
  "vec3 cM = vec3(texture2D(uVideo, effectUv + vec2(0.01, 0.0)).r, baseColor.g, texture2D(uVideo, effectUv - vec2(0.01, 0.0)).b); outColor.rgb = mod(cM * 5.0 + uTime, 1.0);",
  "vec3 cM = vec3(texture2D(uVideo, effectUv + vec2(0.01, 0.0)).r, baseColor.g, texture2D(uVideo, effectUv - vec2(0.01, 0.0)).b); outColor.rgb = 1.0 - cM;",
  "vec3 cM = vec3(texture2D(uVideo, effectUv + vec2(0.01, 0.0)).r, baseColor.g, texture2D(uVideo, effectUv - vec2(0.01, 0.0)).b); outColor.r = dot(cM, vec3(0.393, 0.769, 0.189)); outColor.g = dot(cM, vec3(0.349, 0.686, 0.168)); outColor.b = dot(cM, vec3(0.272, 0.534, 0.131));",
  "vec3 cM = vec3(texture2D(uVideo, effectUv + vec2(0.01, 0.0)).r, baseColor.g, texture2D(uVideo, effectUv - vec2(0.01, 0.0)).b); outColor.rgb = mix(vec3(0.0, 0.2, 0.0), vec3(0.2, 1.0, 0.2), dot(cM, vec3(0.333)));",
  "vec3 cM = vec3(texture2D(uVideo, effectUv + vec2(0.01, 0.0)).r, baseColor.g, texture2D(uVideo, effectUv - vec2(0.01, 0.0)).b); outColor.rgb = mix(vec3(0.2, 0.1, 0.0), vec3(1.0, 0.9, 0.4), dot(cM, vec3(0.333)));"
];
